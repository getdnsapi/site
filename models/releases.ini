[model]
name = Releases
label = Releases
hidden = yes
protected = yes

[fields.order]
label = Sort Order
type = sort_key

[children]
model = release
order_by = -date, record_label
slug_format = getdns-{{ this.version|replace('.', '-') }}

