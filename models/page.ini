[model]
name = Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.order]
label = Sort Order
type = sort_key

[fields.body]
label = Body
type = markdown

[children]
order_by = order, name
